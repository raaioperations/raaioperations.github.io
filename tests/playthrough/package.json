{
  "name": "raai-agent-playthrough-pilot",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ci": "RAAI_PERF_MODE=ci-structural playwright test",
    "test:reference": "RAAI_PERF_MODE=reference playwright test"
  },
  "devDependencies": {
    "@playwright/test": "latest"
  }
}
