{
  "name": "raai-threejs-test-06d",
  "private": true,
  "version": "0.6.4",
  "description": "Living World memory consequence proof: a late-arriving autonomous actor changes route because persistent local world state still exists",
  "type": "module",
  "engines": {"node": ">=22"},
  "scripts": {"build": "node build.mjs"},
  "dependencies": {"three": "0.186.0"},
  "devDependencies": {"esbuild": "0.25.9"}
}
